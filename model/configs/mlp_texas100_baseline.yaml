metadata:
  config_name: "mlp_texas100_baseline"
  description: "A strong MLP baseline for the Texas100 dataset."
  target_dataset: "Texas100"
parameters:
  model_name: "TabularMLP"
  hidden_dims: [512, 256, 128]
  use_dropout: true
  dropout_rate: 0.4
  use_batch_norm: true
  activation: "relu"
  optimizer_type: "adam"
  learning_rate: 0.001
  weight_decay: 0.0001
  epochs: 100